# Two new causal puzzles — 2026-09-22

Base reviewed: beb76e262c27eac6dce4e7d715bccc28736fda06. Work only in chatgpt/android-lab.

Level 3, The Long Way Round, replaces the recovery funnel with two indispensable bridges and an outbound barrier. The safe route first travels away from home, then doubles back. Removing either bridge must invalidate the route physically.

Level 9, A Weighty Key, requires dropping a heavy body onto a visible pressure plate. Sustained top contact above the minimum mass latches it, a visible cable connects it to a retracting gate, and the friends can traverse the preserved bridge. The solver never reads solutionPinId. The new switch gate artwork follows the actual collision endpoint.

New sensor contracts cover top versus side/below contacts, inadequate mass, multi-switch AND logic, reset and template isolation. New level contracts cover all choices, 30/60/120 Hz and irregular schedules, answer-label independence, necessary mechanisms and +/-4 world-unit spawn perturbations. No production timeout was enlarged.

Executed locally: standalone Kotlin/JVM against current production physics, with small isolated Color/Rect/Offset/haptic adapters. Original campaign contracts: 598 simulations, 6947 checks; original prototype design cases: 135 simulations; counterweight: 160; added switch/return contracts: 218. All four suites passed. These are not Android tests or rendering evidence.

Development caught real geometry defects: the new gate route initially jammed at the exit; the return bridge then failed a +4 x perturbation. The bridge endpoints, hazard boundary and catcher were corrected, not the outcome assertions. The final perturbation contracts pass unchanged.

Native campaign includes an extra screenshot after the actual weight switch latches. Four new wrong choices extend the previous seven to eleven. The wrong-choice method budget grows from 90 to 150 seconds to cover those added cases; individual action assertions retain their time limits. All five native methods still run.

At authoring time Android compilation, fresh recordings and visual review of this revision remain required. Do not reuse older videos as proof of these puzzles. Six boards still use the recovery structure; this is not the final twelve-level difficulty curve.

Local terminal and writes were verified through Remote Desktop Commander. Windows had about 1.9 GB RAM free, so no emulator or full Gradle build was started locally; standalone checks used bounded 384 MB compiler and 256 MB runtime heaps. No other apps were closed, no main changes, no Codex, paid inference or Tripo credits.
